# 实验一任务一、任务二完成说明

## 任务一：项目初始化与依赖配置

- `pom.xml`：配置 Maven Web 项目，补充 MyBatis、MySQL 驱动、Log4j、JUnit4 和 MyBatis-Plus 注解依赖，保证现有 `Emp`、`Dept` 实体类可以正常编译。
- `.mvn/wrapper`、`mvnw`、`mvnw.cmd`：保留 Maven Wrapper，方便在没有单独安装 Maven 的环境中直接构建项目。
- `src/main/webapp/WEB-INF/web.xml`：保留 Web 项目基础入口文件，满足 war 项目结构要求。

## 任务二：MyBatis 环境搭建与基础 CRUD

- `src/main/resources/db.properties`：集中保存数据库连接配置，连接现有 `mybatisdemo1` 数据库；没有在代码中硬编码连接信息。
- `src/main/resources/mybatis-config.xml`：MyBatis 全局配置文件，读取 `db.properties`，配置连接池、事务管理、实体类别名和 Mapper 映射文件。
- `src/main/resources/log4j.properties`：开启控制台日志，便于观察 Mapper 执行的 SQL。
- `src/main/java/com/demo2/mapper/EmpMapper.java`：定义 `emp` 表的基础 CRUD 方法。
- `src/main/resources/mapper/EmpMapper.xml`：按照现有 `emp` 表字段 `empno、ename、job、mgr、hiredate、sal、comm、deptno` 编写 SQL 映射，没有新建表，也没有使用指导书示例中的 `emp_id、emp_name` 字段。
- `src/main/java/com/demo2/util/MyBatisUtil.java`：封装 `SqlSessionFactory` 创建逻辑，统一获取 `SqlSession`。
- `src/main/java/com/demo2/MyBatisEmpDemo.java`：第一个 MyBatis 运行程序，用于查询并打印员工列表。
- `src/test/java/com/demo2/mapper/EmpMapperTest.java`：JUnit 测试类，覆盖员工查询、新增、修改、删除流程。

## 数据库说明

- 使用现有数据库：`mybatisdemo1`
- 使用现有表：`emp`、`dept`
- 本次代码没有创建新表，也没有修改数据库表结构。
- 如果要运行数据库测试，需要先在 `db.properties` 中填写本机 MySQL 密码。

## 验证结果

已执行以下命令验证项目可以编译打包：

```powershell
.\mvnw.cmd -q -DskipTests package
```

执行结果：通过。
